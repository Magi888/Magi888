workflows:
  ios-release:
    name: iOS Release Build

    environment:
      flutter: stable
      xcode: latest

    scripts:
      - name: Get Flutter packages
        script: |
          flutter pub get

      - name: Build signed IPA
        script: |
          flutter build ipa

    artifacts:
      - build/ios/ipa/*.ipa